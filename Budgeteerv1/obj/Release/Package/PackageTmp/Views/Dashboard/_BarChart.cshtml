@model Budgeteerv1.Models.BarChartViewModel


@section page
{
    <script type="text/javascript">
        $(document).ready(function(){
            function populatePlot(IncomeData)
            {
                alert("hello");
                alert(IncomeData);
            }
        })
        //onload event is to use the model data as parameters to populate plot and then plot the bargraph. requires middle action
        

        
        })
     
     </script>
}